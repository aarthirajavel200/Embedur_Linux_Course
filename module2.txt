1) find . -type f -size +1M > large_files.txt

2) sed 's/localhost/127.0.0.1/g' config.txt > updated_config.txt

3) grep "ERROR" log.txt | grep -v "DEBUG" > filtered_log.txt

4) ps aux --sort=-%mem | head -n 2
   kill PID
   kill -9 PID

5) netstat -rn | grep UG | sort
